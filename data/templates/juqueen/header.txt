#!/bin/ksh

#@ job_name = {{ job_name }}
#@ comment = ""
#@ error = {{ log_error_path }}
#@ output = {{ log_path }}
#@ environment = COPY_ALL
{% if notify_start and notify_end and notify_error %}
#@ notification = always
#@ notify_user = {{ notify_user }}
{% elif notify_start and notify_error %}
#@ notification = start,error
#@ notify_user = {{ notify_user }}
{% elif notify_start and notify_end %}
#@ notification = start,end
#@ notify_user = {{ notify_user }}
{% elif notify_end and notify_error %}
#@ notification = end,error
#@ notify_user = {{ notify_user }}
{% elif notify_start %}
#@ notification = start
#@ notify_user = {{ notify_user }}
{% elif notify_end %}
#@ notification = end
#@ notify_user = {{ notify_user }}
{% elif notify_error %}
#@ notification = error
#@ notify_user = {{ notify_user }}
{% else %}
#@ notification = never
{% endif %}
#@ wall_clock_limit = {{ wc_limit }}
#@ bg_size = {{ nodes }}
#@ job_type = bluegene
#@ bg_connectivity = TORUS
#@ queue

cd {{ root_path }}

{% block body %}{% endblock %}
