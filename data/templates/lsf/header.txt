#!/usr/bin/env zsh

cd {{ root_path }}

#BSUB -J {{ job_name }}
#BSUB -o {{ log_path }}
#BSUB -W {{ wc_limit }}
{% if notify_start %}#BSUB -B
{% elif notify_end %}#BSUB -N
{% endif %}
#BSUB -u {{ notify_user }}
#BSUB -M {{ memory_per_process }}
#BSUB -n {{ nodes }}

### Uncomment this if you use IntelMPI
##BSUB -a intelmpi
##module switch openmpi intelmpi

{% block body %}{% endblock %}
